# MCP Server Template Configuration Example
# Copy this file to .env and modify the values as needed

# =============================================================================
# API Configuration
# =============================================================================

# API authentication token (required for most operations)
# Set this to your actual API token
API_TOKEN=your_api_token_here

# =============================================================================
# Server Configuration
# =============================================================================

# Server host address (default: 0.0.0.0)
# Use 0.0.0.0 to bind to all interfaces (accessible from other machines)
# Use 127.0.0.1 to bind only to localhost (only accessible from this machine)
HOST=0.0.0.0

# Server port number (default: 8000)
# Choose a port between 1-65535 that's not in use
PORT=8000

# Logging level (default: info)
# Available levels: debug, info, warning, error, critical
# Use debug for development, info for production
LOG_LEVEL=info

# MCP transport protocol (default: sse)
# Available transports: sse, http-streaming
# - sse: Server-Sent Events transport
# - http-streaming: HTTP streaming transport
TRANSPORT=sse

# =============================================================================
# CORS Configuration
# =============================================================================

# CORS allowed origins (default: ["*"])
# Comma-separated list of allowed origins for cross-origin requests
# Use ["*"] to allow all origins (not recommended for production)
# Use specific domains for better security: ["https://example.com", "https://app.example.com"]
CORS_ALLOW_ORIGINS=["*"]

# CORS allow credentials (default: true)
# Set to true to allow cookies and authentication headers in CORS requests
CORS_ALLOW_CREDENTIALS=true

# CORS allowed methods (default: ["*"])
# Comma-separated list of allowed HTTP methods
# Use specific methods for better security: ["GET", "POST", "PUT", "DELETE"]
CORS_ALLOW_METHODS=["*"]

# CORS allowed headers (default: ["*"])
# Comma-separated list of allowed HTTP headers
# Use specific headers for better security: ["Content-Type", "Authorization"]
CORS_ALLOW_HEADERS=["*"]

# =============================================================================
# Environment File Configuration
# =============================================================================

# Path to environment file (default: .env)
# Override this if your .env file is located elsewhere
ENV_FILE=.env

# =============================================================================
# Development Configuration
# =============================================================================

# Enable auto-reload for development (default: false)
# Set to true to automatically restart the server when code changes
# Only use this during development, not in production
# This is typically set via CLI argument: --reload
# RELOAD=true

# =============================================================================
# Examples for Different Use Cases
# =============================================================================

# Example 1: Development Setup
# Uncomment and modify these values for development
# API_TOKEN=dev_token_12345
# HOST=127.0.0.1
# PORT=8000
# LOG_LEVEL=debug
# TRANSPORT=sse
# CORS_ALLOW_ORIGINS=["http://localhost:3000", "http://localhost:8080"]

# Example 2: Production Setup
# Uncomment and modify these values for production
# API_TOKEN=prod_secure_token_abcdef
# HOST=0.0.0.0
# PORT=80
# LOG_LEVEL=info
# TRANSPORT=sse
# CORS_ALLOW_ORIGINS=["https://yourdomain.com"]
# CORS_ALLOW_CREDENTIALS=true
# CORS_ALLOW_METHODS=["GET", "POST"]
# CORS_ALLOW_HEADERS=["Content-Type", "Authorization"]

# Example 3: Local Testing with HTTP Streaming
# Uncomment and modify these values for HTTP streaming transport
# API_TOKEN=test_token
# HOST=127.0.0.1
# PORT=9000
# LOG_LEVEL=debug
# TRANSPORT=http-streaming

# =============================================================================
# Security Notes
# =============================================================================

# 1. Never commit actual API tokens to version control
# 2. Use different tokens for development and production
# 3. Restrict CORS origins in production environments
# 4. Use appropriate logging levels (info or warning for production)
# 5. Consider using environment-specific .env files (.env.dev, .env.prod)

# =============================================================================
# Quick Start Commands
# =============================================================================

# After copying this to .env and setting your values:

# Install dependencies:
# uv sync --all-extras

# Run with default settings:
# python -m src.entry

# Run with SSE transport (default):
# python -m src.entry --transport sse --port 8000

# Run with HTTP streaming transport:
# python -m src.entry --transport http-streaming --port 8000

# Run in integrated mode (MCP + webhook):
# python -m src.entry --integrated --transport sse --port 8000

# Run with debug logging and auto-reload (development):
# python -m src.entry --log-level debug --reload

# Run with custom .env file:
# python -m src.entry --env-file /path/to/custom.env

# Run with token override (CLI takes precedence):
# python -m src.entry --token your_override_token

# =============================================================================
# Testing the Server
# =============================================================================

# Test health endpoint:
# curl http://localhost:8000/health

# Test MCP SSE endpoint:
# curl http://localhost:8000/sse

# Test MCP HTTP streaming endpoint:
# curl http://localhost:8000/mcp

# View API documentation:
# http://localhost:8000/docs (Swagger UI)
# http://localhost:8000/redoc (ReDoc)
